# Concert App - Backend API

Backend API for the Concert Ticketing Application built with Node.js and Express.

## Setup
1. `npm install`
2. Copy `.env.example` to `.env` and configure
3. `npm start`
